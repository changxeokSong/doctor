# jungwoo/app.py 전용 — 메인 프로젝트(../requirements.txt)와 완전히 분리된 독립 앱.
# GPU 경합을 피하려고 torch는 CPU 전용 wheel로 설치한다(메인 앱이 이미 GPU를 거의 다 씀).
streamlit==1.51.0
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1
openpyxl==3.1.5
rank_bm25==0.2.2
sentence-transformers==5.6.0
kiwipiepy==0.23.2
kiwipiepy_model==0.23.0
